export async function loadStickers(categoryPrefixes?: string | string[]) {
  // Logic removed during migration to Convex
  return [];
}

export async function loadStickersByCategory(categoryCode: string) {
  // Logic removed during migration to Convex
  return new Map<string, any[]>();
}

export async function loadStickersBySubcategory(subcategoryCode: string) {
  // Logic removed during migration to Convex
  return [];
}

export async function loadPokemonSubcategories() {
  // Logic removed during migration to Convex
  return [];
}

export async function loadSubcategoryByCode(code: string) {
  // Logic removed during migration to Convex
  return null;
}

export async function loadPokemonSubcategoryGroups(parentCode: string) {
  // Logic removed during migration to Convex
  return [];
}
